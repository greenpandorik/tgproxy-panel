/**
 * Brand constants shared by the shell, the login page and the document title.
 * They mirror `internal/branding/defaults.go` and migration 00008: the API
 * normally supplies the operator's values, and these are what the UI shows
 * while that request is in flight or when it fails. See docs/design/brand.md.
 */
export const DEFAULT_PANEL_NAME = 'TGProxy Panel';
export const DEFAULT_PRIMARY_COLOR = '#e23c92';
export const DEFAULT_ACCENT_COLOR = '#12a198';
