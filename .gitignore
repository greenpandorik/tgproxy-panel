# Secrets / local env files — never commit real MASTER_KEY/SESSION_SECRET etc.
.env
deploy/.env
!.env.example

# Build artifacts / local data
data/
web/node_modules/
web/dist/
/panel
tgwp-agent*

# Editor/tooling scratch
.superpowers/
.claude/
# Installed agent skills are third-party content fetched by `npx skills add`;
# skills-lock.json records which ones this project uses.
.agents/
