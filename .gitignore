# Secrets / local env files — never commit real MASTER_KEY/SESSION_SECRET etc.
.env
deploy/.env
!.env.example

# Build artifacts / local data
data/
web/node_modules/
# Vite's root-level cache, and the typescript symlink the TS language server
# resolves from the repo root (the SPA's own packages live in web/).
/node_modules/
web/dist/
/panel
tgwp-agent*

# Editor/tooling scratch
.superpowers/
.claude/
.agents/
.impeccable/
.playwright-mcp/
.playwright-cli/
skills-lock.json
/output/
