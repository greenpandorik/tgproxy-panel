version: "2"
linters:
  default: standard
  enable: [errcheck, govet, staticcheck, unused, ineffassign, misspell]
formatters:
  enable: [gofumpt]
  settings:
    gofumpt:
      module-path: tgwebproxy
