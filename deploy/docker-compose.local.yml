# Local mode (install.sh --local): no domain, no Caddy, no TLS. The panel is published
# directly on :8080. Used together with docker-compose.release.yml:
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up -d
services:
  panel:
    ports: ["8080:8080"]
